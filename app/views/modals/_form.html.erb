<%= form_for(@modal) do |f| %>
  <% if @modal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@modal.errors.count, "error") %> prohibited this modal from being saved:</h2>

      <ul>
      <% @modal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :header %><br />
    <%= f.text_field :header %>
  </div>
  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_area :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
